'use client';
import type { ColumnDef } from '@tanstack/react-table';

// This file was incorrectly generated and is not currently used.
// It is being replaced with a valid empty export to prevent build errors.
export const unusedColumns: ColumnDef<any>[] = [];
